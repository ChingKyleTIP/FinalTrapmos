specVersion: "v1beta"
serviceId: "trapmos"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "trapmos-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
